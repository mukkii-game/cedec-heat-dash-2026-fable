# CEDEC HEAT DASH 2026 アートディレクション

## 0. ゴール

**「1985年のアーケード筐体に入っていても、2026年のスマホで遊んでも成立するドット絵」**

- レイアウトの参考はベルトスクロール型ラン&アクションの古典構図
  （上=壁面背景 / 中=奥行きのある床 / 下=HUD帯）のみ。絵柄・配色・キャラは完全新規。
- リアルより漫画的。暑いが陰鬱ではない。「白い光・濃い影・彩度の高い夏」。

## 1. 解像度・スケール規律

- 内部解像度 **480×270**（16:9）。全ドット絵はこのピクセル格子上に置く。
- 拡大は最近傍のみ（imageSmoothing無効）。回転は原則禁止（例外: カモメ・回転草・紙吹雪等の小物）。
- レイアウト（横画面時）:
  - y 0–99: 空＋遠景＋中景（パララックス3層）
  - y 100–233: 床（z=0奥 → z=1手前 の疑似奥行き。奥ほどスプライト縮小 0.62→1.0）
  - y 234–270: HUD帯（黒地）
- サブピクセル移動はカメラのみ許可。スプライト描画は整数座標にスナップ。

## 2. マスターパレット（基本32色＋日別ランプ）

輪郭・影に**純黒を使わない**。共通アウトラインは `#221833`（夜紫）。

キャラクター共通:
- 肌 `#f2c193` / 影 `#cf8e62`
- ミナト: キャップ赤 `#e8504b`/`#b03042`、シャツ水色 `#3ec6c0`/`#2b8f92`、
  パンツ紺 `#2e4a7a`、靴 `#f5f1e8`、リュック橙 `#f2a33c`/`#c47a24`、パスケース桃 `#e84b8a`
- モブ: 彩度を主人公より1段落とす（主人公が常に最も目立つ）

WAVEごとのカラースクリプト（世界の温度が上がっていく。旧DAY1〜3のテーマをそのまま
チェックポイントで切り替えて使う）:

| | 空 | 路面 | ビル | キーカラー |
|---|---|---|---|---|
| WAVE1 爽快 | `#8fd6ff`→`#e8f8ff` | `#e8d9b8` 目地`#cbb694` | 青灰 `#b8c4d8`/`#8fa0bd` ガラス`#bfe8f2` | シアン |
| WAVE2 灼熱 | `#ffd98a`→`#fff3d0` | `#edcf9a` 照り返し`#fff3c2` | 暖色化 `#d8c0a8`/`#b09680` | アンバー |
| WAVE3 砂漠 | `#ff9e4f`→`#ffd166` | 砂 `#edc57e`/`#d9a45c` | 半ば砂に埋没・遠景紫 `#9b6a9e` | オレンジ×紫 |

- 日向/日陰: 日陰は**専用暗色パレット**（単純な半透明黒でなく、青紫 `#4a5a8f` 系に転がした乗算+輪郭維持）。
  境界はギザギザのピクセル階段で「ビルの形の影」を落とす。床の影形状＝ゲーム情報なので視認性最優先。
- 照り返しタイル: 白 `#fff8dc` の明滅＋上向きの陽炎パーティクル。

## 3. キャラクター規律

- 頭身: 2.5頭身（頭大きめ＝表情が読める）。主人公 約22×30px。
- 輪郭: 1px `#221833` 全周。内部の色分割線は省略し色面で見せる。
- 影: セル2段（基本色+影色）。ハイライトは白1pxを帽子・靴など要所のみ。
- アニメ: 走り6フレーム(10fps、速度で再生速度可変)、ジャンプ2、よろけ2、
  名刺交換1、倒れ3、ゴール万歳2、待機2。汗しずく・熱波オーラは別パーツ合成。
- モブは3色レシピ×色替えで量産するが、シルエットを3種以上（日傘、スーツケース、カメラ）。
  通行人・買い物客系は主人公比でさらに一段階大きく描画し、障害物としての存在感を強める
  （当たり判定はメートル系のまま変えていないため、難易度は変えずに「読みやすさ」だけ上げる）。
- 迷惑キックボード：法定速度を無視して爆走する、なびく長い髪とワンピースの女性。
  単発でも複数台の同時出現（横並びで爆走する「スワーム」）でも登場する。

## 4. 背景密度

- 遠景（視差0.1）: 空グラデ帯＋観覧車系ランドマークのシルエット＋海。
- 中景（視差0.3）: ビル群。窓は2px格子で省略しつつ、ガラスに空色の反射1列。
- 近景の壁（視差0.6）: 街路レベルの店構え・植栽・看板（開発者ネタはここ）。
- 床（視差1.0）: タイル目地の斜めスクロール＋影・砂・照り返しゾーン。
- 「密度は面積でなくリズム」: 20〜40mごとに視線の引っかかり（看板/店/旗）を1つ。

## 5. UI・フォント

- HUD帯: `#14101f` 地。上辺に1pxのシアンライン（筐体感）。
- 数字・英字: 自作8×8ビットマップフォント（角ばったアーケード体、オリジナル字形）。
- 日本語: Yusei Magic（OFLライセンス、THIRD_PARTY_ASSETS.mdに記録）。
  取得不能時はシステムフォント小サイズ→最近傍拡大の疑似ドットにフォールバック。
  取得できた場合も完全な2値化はせず、高解像度で描いてから縮小＋なだらかな
  コントラスト強調に留め、フォント本来の丸みを残して可読性を優先する。
- HUD構成: WAVE表示 / TIME(経過、★評価用の計測のみ) / AREAバー(進行度＋コンビニ・
  ゴールのマーカー) / ミュート・ポーズ。**ヒートゲージはHUDに置かない**。主人公の
  スプライト自体が足元から頭へ赤みがせり上がる形でヒートを表現し（危険域では
  明滅も追加）、頭のてっぺんまで赤く染まると熱中症で倒れる。チェックポイント通過時は
  数秒だけ「WAVE 2」等のバナーをプレイを止めずに重ねる。
- セリフ: 吹き出しは白地+夜紫枠、しっぽ付き、2秒で消える。

## 6. エフェクト

- 走行: 足元の土埃(2〜3px粒)、最高速時のスピードライン（画面端1pxストリーク）。
- 日向: ゆらゆら上昇する陽炎パーティクル（1px縦揺れ）。WAVE3は画面全体に微小な走査歪み。
- ヒートが上がるほど濃くなる継続的な湯気パーティクルがプレイヤーから立ち上り、
  90%付近では「もうもうもう」と量・粒サイズともに増して包まれるように見せる
  （汗・土埃の一発演出とは別枠の常時エフェクト）。
- 日陰に居続けている間は爽やかな青いキラキラ粒＋「シュワー」という炭酸質の
  SEを繰り返し、回復し続けている実感を出す（進入時の一発演出は別に残す）。
- 太陽: 常につり眉・目・口の「顔」を持つ。日射レーザーの警告〜照射に連動して
  真っ赤に変色し、目は赤く光り口は牙のように開く（凶悪な表情）。
- レーザー: 予告=地面に点滅照準帯+空から光柱の予兆線 → 照射=炎をまとった巨大な柱が
  ぎざぎざの縁を揺らめかせながら落下（着弾の瞬間だけ地面に衝撃波の輪）。
- 日陰進入: 青い「ホッ」パーティクル＋SE。ミスト: 白い霧粒。
- ゴール: 紙吹雪＋タイム表示のカウントアップ＋ランクスタンプ(ドン!)。
- 画面フラッシュは輝度70%以下・2フレーム以内（光過敏対策）。

## 7. 禁止事項

- 素の矩形/円/絵文字/フォント記号を「完成素材」として画面に出さない
  （デバッグ表示を除く。UI枠も必ずピクセル装飾を施す）。
- 純黒 `#000` の面積使用禁止（HUD地は夜紫）。ドロップシャドウのぼかし禁止（ドット絵の解像度感を守る）。
- 既存ゲームのキャラ・ロゴ・配色・固有演出の再現禁止。reference画像は構図研究のみ。
