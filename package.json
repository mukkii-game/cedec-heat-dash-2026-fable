{
  "name": "cedec-heat-dash-2026",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "CEDEC HEAT DASH 2026 - a retro pixel-art heat-survival runner. Walk 5 min, feels like 30.",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
